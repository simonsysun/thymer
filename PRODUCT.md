# Work Rest Timer

状态：2026-09-07 的产品草案；原生 SwiftUI 首页交互预览阶段，尚未完成可日常使用的记录产品。

## 要解决的问题

帮助在电脑前学习、工作的人，随手开始记录当前任务，在连续工作一段时间后起身休息，再继续下一轮。减少手工记时间和反复设置计时器的负担；暂停、换任务、忽略提醒时，记录也应该可理解。

首页的核心是一个可拨动的圆形计时器。Mac 菜单栏入口、小弹窗、播放／暂停、双拨杆联动和时间逐渐消耗的动效，共同构成产品，而非一个复杂的任务管理系统。

## 已确认的要求

- 当前仅做 Mac，手机和跨设备同步不在此阶段范围内。
- 点击右上方菜单栏图标，展开紧凑面板；关闭面板不代表暂停。
- 中央使用播放／暂停图标，下方放当前任务输入框；用少量文字解释必要状态。
- 一圈始终代表 60 分钟，工作时长可以短于或长于一圈。
- 外侧红色拨杆设定工作时长；内侧浅色拨杆设定随后休息的时长。
- 移动工作拨杆会带动休息拨杆，保持休息时长；移动休息拨杆只改变休息时长。
- 工作期间红色弧段逐渐消耗；进入休息后，浅色弧段逐渐变色。动效克制、顺滑。
- 暂停保留本轮进度；恢复后继续。Reset 用小的回转箭头表示，方便开始新任务。
- 每天按任务累计实际计时中的工作时间；休息、暂停、等待开始不计入学习／工作总量。
- 工作结束自动进入休息；是否在休息结束后自动开始下一轮工作可设置。
- 阶段切换提供视觉提醒。声音可以关闭，视觉提醒没有总开关；关闭自动开始时尤其不能静默停下。
- 自动开始下一轮也发出简短提示，让用户知道已经开始计时。

## 本轮采用的设计假设

这些是可在预览中调整的方案，不冒充用户已明确决定的要求。

| 项目 | 预览方案 | 理由 |
| --- | --- | --- |
| 初始时长 | 工作 50 分钟，休息 10 分钟 | 对应用户描述的常见使用场景，便于看完整一圈 |
| 自动开始下一轮 | 默认开启 | 呼应用户担心错过提醒后漏记时间；可关闭 |
| 提示音 | 默认开启 | 短促轻柔，设置中可静音 |
| 旋转方向 | 从 12 点顺时针增加 | 以熟悉的钟表方向降低理解成本 |
| 时长范围 | 工作 1–120 分钟，休息 1–30 分钟 | 预览覆盖多圈和跨零点，不代表最终产品上限 |
| 拨杆编辑 | 仅准备状态可编辑 | 避免更改已经开始记录的区间；运行／暂停期间先 Reset |
| Reset | 结束当前段、保留累计，回到完整的当前设定，等待开始 | “回去”暂解释为回到本轮初始设定；不擦除历史，也不强迫每次重新拨时长 |

## 首页和设置的范围

首页仅包含：菜单栏入口、阶段状态、双拨杆圆盘、中央开始／暂停、剩余时间、Reset、当前任务、今日工作累计，以及设置入口。

设置只包含两个开关：**提示音**、**休息后自动开始工作**。阶段提醒是一条固定规则，不再提供一个能导致静默等待的总通知开关。

历史列表、图表、任务项目管理、账号、同步、手机、长休息策略、连续天数和成就不在本次预览内。任务名由用户指定，系统自动累计该任务的计时，不自动推断正在学习哪项内容。

## 阶段和计时规则

| 当前状态 | 事件 | 下一状态 | 记录与反馈 |
| --- | --- | --- | --- |
| 准备 | 点击播放 | 工作 | 建立工作区间 |
| 工作 | 点击暂停 | 工作暂停 | 结算至暂停时刻；余时冻结 |
| 工作暂停 | 点击播放 | 工作 | 续记当前任务；不补记暂停时段 |
| 工作 | 到时 | 休息 | 结算工作；提示“起来活动一下”；按声音设置响一声 |
| 休息 | 点击暂停 | 休息暂停 | 余时冻结；不计入工作累计 |
| 休息暂停 | 点击播放 | 休息 | 继续剩余休息 |
| 休息 | 到时，自动开始开 | 工作 | 下一轮开始；提示“新一轮开始了”；可响一声 |
| 休息 | 到时，自动开始关 | 等待开始 | 工作累计停止；提示“休息结束，点击开始”；菜单栏保留等待标记 |
| 等待开始 | 点击播放 | 工作 | 开始完整新一轮，不把等待时间补记进去 |
| 任意状态 | Reset | 准备 | 保留已累计时间，放弃本轮剩余时间，允许换任务；不自动启动 |
| 工作／休息 | 收起弹窗 | 原状态 | 持续计时，菜单栏显示余时与阶段 |

等待开始与用户主动暂停具有不同含义。前者下一次开始完整工作阶段，后者继续尚未完成的阶段。用“待开始”和“已暂停”区分，不以相同的冻结数字掩盖差别。

任务名可留空，记录为“未命名任务”；运行中及暂停中禁止直接改名，Reset 后可切换，已记录的区间仍属于原任务。

## 提醒约定

| 事件 | 视觉内容 | 持续性 |
| --- | --- | --- |
| 工作结束 | “起来活动一下”／“休息 10 分钟” | 短提示，面板切换到休息状态 |
| 休息结束，自动开始 | “新一轮开始了”／任务名 | 短提示，菜单栏开始倒计时 |
| 休息结束，手动开始 | “休息结束，点击开始” | 通知可消失；菜单栏等待标记和面板提示持续到操作 |

声音开关只影响提示音，不影响视觉状态和计时。关闭弹窗仍应有阶段提醒；不强制抢走输入焦点，不持续闪烁，不连续重复响铃。

正式版的系统通知受通知权限和系统专注模式限制，不能承诺强制送达。应用内状态与菜单栏等待标记必须独立于系统通知存在；首次启用时应诚实说明权限被拒的状态。此次原生预览用应用自有的轻提示浮层，不接入通知中心，也不向系统申请通知权限。

## 时间记录的边界

原生产品需要本地持久保存工作区间，而不是只保存一个容易失真的总数。每段至少记录任务标识、开始／结束时刻、有效工作时长和结束原因；日汇总按本地日期切分跨午夜区间。

实际计时以时钟差值为准，显示刷新频率不作为时间真值。休眠、锁屏、重启、系统时间修改和重复启动需要单独验证。原生版建议锁屏／休眠时暂停工作，唤醒后明确等待继续；不因没有鼠标键盘操作就断定没有学习。

预览只使用当前进程内的临时数据，退出归零，不写学习记录到真实工作数据。加速演示不产生可用于统计的真实记录，单独的预览控制窗口和累计区域明确标注这一点。

## 本次预览应回答什么

1. 弹窗在实际的小尺寸下是否清楚、紧凑？中央按钮是否一眼能认出？
2. 红色带动浅色拨杆是否自然，休息间隔是否保持不变？
3. 红色消耗、浅色变为休息色、下一轮恢复的顺序能否直接看懂？
4. 暂停、等待开始和阶段切换是否足够明显？静音时仍能知道计时状态吗？

用户最新要求明确只为 Mac 开发，直接使用 SwiftUI 与当前系统适配的原生 UI。预览在真实菜单栏中运行，但预览通过不代表通知中心、持久记录和长期后台可靠性已经验证。
